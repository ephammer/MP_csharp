<Window x:Class="WpfApp.SpecializationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="SpecializationWindow" Height="300" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*" />
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Button 
            Content="Add" 
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Width="50" 
            Click="Add_Button_Click"
            Grid.Column="1" Margin="0,0,0,5"/>
        <Button 
            Content="Remove"
            HorizontalAlignment="Right" 
            VerticalAlignment="Top" 
            Width="50"
            Grid.Column="1"
            Grid.Row="1" Margin="0,0,0,5" Click="Remove_Button_Click"/>
        <Button
            Content="Update"
            HorizontalAlignment="Right"
            Width="50" 
            Grid.Row="2"
            Grid.Column="1" Margin="0,1" Click="Update_Button_Click"/>
        <DataGrid 
            Grid.RowSpan="3"
            Height="auto"
            Width="auto"
            Name="ListSpecialization" 
            SelectionChanged="ListSpecialization_SelectionChanged"
            Loaded="ListSpecialization_Loaded"
            IsReadOnly="True"
            >

        </DataGrid>


    </Grid>
</Window>
